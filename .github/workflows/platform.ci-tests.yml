name: ".Platform - Run CI tests"

on:
  workflow_dispatch:
<<<<<<< HEAD
=======
    inputs:
      testFileRegex:
        type: string
        description: "The regex of the test file(s) to run"
        required: false
        default: ".*"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/platform.ci-tests.yml"
      - "utilities/**"
      - "!*/**/README.md"
  schedule:
    - cron: "0 0 * * Sun" # Every Sunday
>>>>>>> main

env:
  workflowPath: ".github/workflows/platform.ci-tests.yml"

jobs:
  test_classic:
    runs-on: ubuntu-latest
<<<<<<< HEAD
    name: "Run CI tests (classic)"
=======
    name: "Initialize pipeline"
    if: ${{ !cancelled() && !(github.repository != 'Azure/bicep-registry-modules' && github.event_name != 'workflow_dispatch') }}
    steps:
      - name: "Checkout"
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - name: "Set input parameters to output variables"
        id: get-workflow-param
        uses: ./.github/actions/templates/avm-getWorkflowInput
        with:
          workflowPath: "${{ env.workflowPath}}"
    outputs:
      workflowInput: ${{ steps.get-workflow-param.outputs.workflowInput }}

  ###############
  #   Removal   #
  ###############
  job_run_tests:
    runs-on: ubuntu-latest
    name: "Run CI tests"
    needs:
      - job_initialize_pipeline
>>>>>>> main
    steps:
      - name: "Checkout"
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: "main"

      - name: Set environment
        uses: ./.github/actions/templates/avm-setEnvironment

      - name: Run CI tests
        id: pester_run_step
        uses: azure/powershell@v2
        with:
          inlineScript: |
            # Load used functions
            . (Join-Path $env:GITHUB_WORKSPACE 'utilities' 'tools' 'Set-AVMModule.ps1')

            $inputObject = @{
              ModuleFolderPath       = (Join-Path $env:GITHUB_WORKSPACE 'avm' 'res' 'network')
              SkipFileAndFolderSetup = $true
              SkipVersionCheck       = $true
              Recurse                = $true
            }

            $StartTime = Get-Date
            Set-AVMModule @inputObject
            $ElapsedTime = (Get-Date) - $StartTime
            $TotalTime = '{0:HH:mm:ss}' -f ([datetime]$ElapsedTime.Ticks)
            Write-Verbose ("Execution took [$TotalTime]") -Verbose

            git diff --stat --diff-filter=AM
          azPSVersion: "latest"

  # test_rpc:
  #   runs-on: ubuntu-latest
  #   name: "Run CI tests"
  #   steps:
  #     - name: "Checkout"
  #       uses: actions/checkout@v5
  #       with:
  #         fetch-depth: 0

  #     - name: Set environment
  #       uses: ./.github/actions/templates/avm-setEnvironment

  #     - name: Run CI tests
  #       id: pester_run_step
  #       uses: azure/powershell@v2
  #       with:
  #         inlineScript: |
  #           # Load used functions
  #           . (Join-Path $env:GITHUB_WORKSPACE 'utilities' 'tools' 'Set-AVMModule.ps1')

  #           $inputObject = @{
  #             ModuleFolderPath       = (Join-Path $env:GITHUB_WORKSPACE 'avm' 'res' 'network')
  #             SkipFileAndFolderSetup = $true
  #             SkipVersionCheck       = $true
  #             Recurse                = $true
  #           }

  #           $StartTime = Get-Date
  #           Set-AVMModuleClassic @inputObject
  #           $ElapsedTime = (Get-Date) - $StartTime
  #           $TotalTime = '{0:HH:mm:ss}' -f ([datetime]$ElapsedTime.Ticks)
  #           Write-Verbose ("Execution took [$TotalTime]") -Verbose

  #           git diff --stat --diff-filter=AM
  #         azPSVersion: "latest"

  # test_rpc_async:
  #   runs-on: ubuntu-latest
  #   name: "Run CI tests (async)"
  #   steps:
  #     - name: "Checkout"
  #       uses: actions/checkout@v5
  #       with:
  #         fetch-depth: 0

  #     - name: Set environment
  #       uses: ./.github/actions/templates/avm-setEnvironment

  #     - name: Run CI tests
  #       id: pester_run_step
  #       uses: azure/powershell@v2
  #       with:
  #         inlineScript: |
  #           # Load used functions
  #           . (Join-Path $env:GITHUB_WORKSPACE 'utilities' 'tools' 'Set-AVMModule.ps1')

  #           $inputObject = @{
  #             ModuleFolderPath       = (Join-Path $env:GITHUB_WORKSPACE 'avm' 'res' 'network')
  #             SkipFileAndFolderSetup = $true
  #             SkipVersionCheck       = $true
  #             Recurse                = $true
  #             Async                  = $true
  #           }

  #           $StartTime = Get-Date
  #           Set-AVMModuleClassic @inputObject
  #           $ElapsedTime = (Get-Date) - $StartTime
  #           $TotalTime = '{0:HH:mm:ss}' -f ([datetime]$ElapsedTime.Ticks)
  #           Write-Verbose ("Execution took [$TotalTime]") -Verbose

  #           git diff --stat --diff-filter=AM
  #         azPSVersion: "latest"
